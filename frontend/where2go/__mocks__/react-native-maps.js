const React = require('react');

const MapView = ({ children }) => {
    return <>{children}</>;
};

const Marker = () => null;

module.exports = {
    __esModule: true,
    default: MapView,
    Marker,
};